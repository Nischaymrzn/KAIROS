# requirements.lock.txt — the EXACT environment that produced the v7 results.
# Captured 2026-08-05 from the working Python 3.10.0 / Windows / RTX 3050 env.
#
# requirements.txt states intent (what the project needs).
# THIS file states fact (what it was proven to run on). Cite this one in the
# methodology: it is what makes the results reproducible.
#
# Install:
#   pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.lock.txt
#
# (torch is pinned separately because the +cu124 build comes from the PyTorch
#  index, not PyPI. For a CPU-only machine use the /whl/cpu index instead; every
#  other pin is unchanged and CPU-safe.)

# --- core ---
numpy==1.26.4
pandas==2.3.1
pyarrow==24.0.0
scipy==1.15.3
scikit-learn==1.7.2
joblib==1.5.3
PyYAML==6.0.2
tqdm==4.67.1

# --- gradient boosting ---
xgboost==3.2.0
lightgbm==4.6.0
catboost==1.2.10

# --- deep learning (see note above re: the +cu124 index) ---
torch==2.6.0

# --- explainability + experiment tracking ---
shap==0.49.1
mlflow==3.14.0

# --- data sources ---
nba_api==1.11.4
requests==2.32.4

# --- archive handling (SportVU .7z game files) ---
# Imported by src/movement/extract.py; was MISSING from requirements.txt until
# 2026-08-05, so `make setup` could not run any tracking extraction.
py7zr==1.1.3

# --- trajectory work ---
tslearn==0.9.0

# --- hyperparameter tuning ---
optuna==4.9.0

# --- inference + game API ---
fastapi==0.133.0
uvicorn==0.38.0
pydantic==2.11.10
pydantic-settings==2.10.1
sqlmodel==0.0.39

# --- plotting ---
matplotlib==3.10.3
seaborn==0.13.2

# --- tabular deep learning (model-comparison chapter) ---
rtdl_revisiting_models==0.0.2
pytorch-tabnet==4.1.0
